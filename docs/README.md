# docs

Put `demo.gif` here — a short screen recording of: select lines → press the hotkey → paste the `@…#start-end` reference into Claude Code.

It's referenced from the **Demo** section of the top-level [README](../README.md); uncomment the image line there once the file exists.
